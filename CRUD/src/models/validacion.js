function validar(data){
    const {question,answer1,answer2,answer3,answer4,is,resp,ref, dificultad, status} = data;
  // if(pregunta.length < 3 ){
  //   res.render('index')
  // }
}
module.exports = {
    createUsersValidacion,
};